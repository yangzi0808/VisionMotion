# VisionMotion 项目 Demo 视频录制脚本

> 目标成片：**2 分 30 秒**（可在 2:00–3:00 之间裁剪或扩展）
> 适用场合：实验室本科项目展示 / 课程结题录像 / 项目主页演示
> 事实基线：`master` @ `3e41ff9 docs: update license reference`（2026-09-26）
> 说明：本文档只描述录制流程，**不修改任何代码、数据与文档**；文中全部数值均取自仓库内现有文件（来源见附录 B）。

---

## 0. 素材与事实基线

| 素材 | 路径 | 在片中的用途 | 核对要点 |
| --- | --- | --- | --- |
| 答辩 PPT | `docs/VisionMotion_Final_Presentation.pptx` | 主线画面（第 1–4、10 页） | 10 页，16:9（13.33 × 7.5 in） |
| PPT 备份 | `docs/VisionMotion_Final_Presentation.pdf` | 放映备用（字体不丢版） | 与 PPTX 同版 |
| 图 A | `results/EXP-EXT-LAB67-V1_fig_A_y0_timeseries.png` | 位置时间序列 y₀(t) | 3900 × 1980 px，dpi 300 |
| 图 B | `results/EXP-EXT-LAB67-V1_fig_B_frequency_comparison.png` | 时域 / FFT / 模型三方对照 | 3900 × 1980 px，dpi 300 |
| 图 C | `results/EXP-EXT-LAB67-V1_fig_C_pipeline.png` | 完整技术链总览 | 4800 × 2700 px，dpi 300 |
| 追踪叠加视频（自采） | `results/EXP-004-DYNAMIC-001_overlay.mp4` | 现场可视化主素材 | 29.37 s，1766 帧，960 × 540 |
| 追踪叠加视频（自采） | `results/EXP-003-STATIC-001_overlay.mp4` | 可选补充镜头 | 36.11 s，2171 帧，960 × 540 |
| 单图检测结果 | `results/EXP-001-IMAGE-001_overlay.png` | 终端演示的可见产出 | 1279 × 1706 px |
| 单图检测输入（本地，未随仓库分发） | `data/raw/EXP-001-IMAGE-001.jpg` | S5 命令 1 的输入 | 本机存在；`git ls-files data/` 只有 `.gitkeep` |
| 位移曲线 | `results/EXP-004-DYNAMIC-001_displacement.png` | 可选扩展镜头 | 1800 × 900 px |
| 最终报告 | `docs/M6.3_FINAL_REPORT.md` | 镜头 S8 展示 | 354 行，14 节 + 附录 A |
| 数据来源说明 | `docs/ATTRIBUTION.md` | 镜头 S3、S8 展示 | 第三方署名与许可 |
| 作者简介 | `docs/AUTHOR_PROFILE.md` | 字幕口径依据 | 姓名栏为"待填写"，**不要虚构姓名** |
| GitHub 仓库 | <https://github.com/yangzi0808/VisionMotion> | 镜头 S8 展示 | 默认分支 `master` |

---

## 1. 录制规格

| 项目 | 设定 |
| --- | --- |
| 画面 | 1920 × 1080，30 fps（终端文字滚动少时可用 60 fps） |
| 屏幕缩放 | 100%；关闭夜间模式与自动亮度 |
| 终端 | Windows Terminal + PowerShell 7，深色主题，字号 16–18 |
| 终端编码 | 开录前执行 `chcp 65001`，避免中文输出乱码 |
| 浏览器 | 无痕窗口最大化，缩放 125%，隐藏书签栏与扩展图标 |
| 图片查看 | Windows 照片 或 VS Code 图片预览，用 100% 缩放平移指认标注 |
| 音频 | 独立麦克风，48 kHz；先录 5 秒静音作为底噪样本 |
| 录制工具 | OBS / Xbox Game Bar / PowerPoint 录制；**按镜头分段录制**，便于单独重录 |
| 导出 | MP4（H.264）+ AAC 192 kbps，8–12 Mbps |

---

## 2. 录制前准备

1. 打开 4 个窗口并固定切换顺序：**PPT → 终端 → 图片查看器 → 浏览器**。
2. 终端先执行一次以下内容（不录入成片）：

   ```powershell
   chcp 65001
   Clear-Host
   Set-Location 'C:\Users\杨千贤\Desktop\VisionMotion'
   ```

3. 把 S5 的演示命令预输入或复制到剪贴板，**不要在镜头前逐字敲长路径**。
4. 关闭通知（专注助手）、微信 / QQ / 邮件弹窗。
5. 素材预检：

   ```powershell
   git status --short --untracked-files=no   # 预期：无输出（只检查已跟踪文件）
   git log --oneline -1                      # 预期：3e41ff9 docs: update license reference
   ```

   > 说明：本录制脚本 `demo_video_script.md` 是**未跟踪的新文件**，直接执行 `git status --short` 会多出一行
   > `?? demo_video_script.md`，属于正常现象，不代表仓库内容被改动。`--untracked-files=no` 只检查**已跟踪文件**，
   > 输出为空才代表"仓库零改动"。若希望画面绝对干净，可在录制前先提交该脚本。

6. 备份 `results/`（可选但推荐）：把整个 `results` 目录复制一份到仓库外，录制结束用于比对。
7. 完整预演 3 次并计时，确认旁白不超时。

---

## 3. 时间轴总览

| 镜头 | 起止 | 时长 | 场景 | 主要素材 | 折合字数 | 预计朗读 |
| --- | --- | --- | --- | --- | --- | --- |
| S1 | 0:00–0:10 | 10 s | PPT 第 1 页 | 标题页 | 34 | 8.7 s |
| S2 | 0:10–0:28 | 18 s | PPT 第 2 页 | 研究问题 + 核心链路 | 65 | 16.6 s |
| S3 | 0:28–0:46 | 18 s | PPT 第 3 页 → 编辑器 | 数据来源与许可 | 63 | 16.1 s |
| S4 | 0:46–1:02 | 16 s | 图 C 全屏 | 技术链总览 | 58 | 14.8 s |
| S5 | 1:02–1:24 | 22 s | 终端 | 现场运行 + 复现自检 | 71 | 18.1 s |
| S6 | 1:24–1:48 | 24 s | 图 A → 图 B | 测量结果与三方对照 | 87 | 22.2 s |
| S7 | 1:48–2:00 | 12 s | 叠加视频播放 | tracking overlay | 44 | 11.2 s |
| S8 | 2:00–2:14 | 14 s | 浏览器 | GitHub 仓库页 | 49 | 12.5 s |
| S9 | 2:14–2:30 | 16 s | PPT 第 10 页 | 结论、局限、收尾 | 59 | 15.1 s |
| 合计 | 0:00–2:30 | 150 s | — | — | 530 | 135.3 s |

> 计数规则：折合字数 = 汉字数 + 数字字符数 + 英文词数 × 2（连字符缩写如 `CC BY-NC-SA` 按一个英文词计）；预计朗读按 **235 字/分钟**，可读区间 210–250 字/分钟。
> 按最快的 250 字/分钟计算，全部旁白约 127 s，各镜头均不超时；相对 150 s 总时长留有约 23 s，用于命令粘贴、画面切换与停顿。

---

## 4. 分镜脚本

### S1 片头｜0:00–0:10（10 s）

- **录屏画面**：PPT 第 1 页全屏。标题"VisionMotion"与副标题"基于计算机视觉的低成本非接触式位移与振动监测"完整可见；画面下方叠加链路条"视频 → y₀(t) → T → f → FFT → SHM 参考"。鼠标静止不晃动。
- **终端命令**：无。
- **需要展示的文件**：`docs/VisionMotion_Final_Presentation.pptx` 第 1 页。
- **字幕叠加**：项目名 + "深圳大学 光电信息科学与工程专业 本科生项目"（依据 `docs/AUTHOR_PROFILE.md`，**不出现真实姓名**）。
- **旁白**：大家好，我是深圳大学光电信息科学与工程专业的本科生。这里展示我的项目 VisionMotion。
- **录制提示**：开机即录，片头不加转场；旁白第一句压住标题出现的瞬间。

### S2 研究问题与核心链路｜0:10–0:28（18 s）

- **录屏画面**：PPT 第 2 页。鼠标依次指向页面上的四个研究问题，再沿"视频 → y₀(t) 位置时间序列 → T 周期 → f 频率 → FFT 交叉验证 → SHM 条件性参考"这条链路慢速划过。
- **终端命令**：无。
- **需要展示的文件**：`docs/VisionMotion_Final_Presentation.pptx` 第 2 页。
- **旁白**：项目想回答一个朴素的问题：普通摄像头拍的视频，能否测出机械目标的振动周期和频率。链路是视频、位置序列、周期、频率、FFT 交叉验证，最后与物理模型对照。
- **录制提示**：强调"不使用深度学习，重点是可解释、可复现"，对应页面上的说明文字。

### S3 数据来源与合规声明｜0:28–0:46（18 s）

- **录屏画面**：先停留在 PPT 第 3 页（来源、条目 ID、质量标签、视频参数、分析区间），随后切到编辑器展开 `docs/ATTRIBUTION.md` 的来源表，停 2–3 秒。
- **终端命令**：无（如需在终端展示，可执行 `Get-Content docs\ATTRIBUTION.md | Select-Object -First 20`，但编辑器展示更清晰）。
- **需要展示的文件**：`docs/VisionMotion_Final_Presentation.pptx` 第 3 页；`docs/ATTRIBUTION.md`。
- **字幕叠加**（建议常驻 3 秒以上）：
  > External public educational dataset · ComPADRE / OSP Entry ID 16081 · Author: Kathleen Koenig · License: CC BY-NC-SA 3.0
- **旁白**：数据必须说清楚：正式分析用的是外部公开教学数据 ComPADRE Lab67 Video 1，不是自主采集，许可为 CC BY-NC-SA 3.0；分析区间第 76 到 235 帧，共 160 帧。
- **录制提示**：本镜头是合规关键镜头，**署名与许可必须同时出现在画面上**；不要在此处播放外部视频连续画面。

### S4 技术链总览｜0:46–1:02（16 s）

- **录屏画面**：图 C 全屏（`results/EXP-EXT-LAB67-V1_fig_C_pipeline.png`），从左上角"计算机视觉层"开始，沿流程箭头平移至"时域测量层 → 频域交叉验证层 → 物理模型"，最后停在右侧结论区。
- **终端命令**：无。
- **需要展示的文件**：`results/EXP-EXT-LAB67-V1_fig_C_pipeline.png`（可选同时翻到 PPT 第 4 页对应检测规则）。
- **旁白**：方法不使用深度学习，完全可解释：固定 ROI 内做灰度阈值、暗连通域与行宽带筛选，取暗带上缘 y₀ 作为位置特征，160 帧全部检出，无缺口。
- **录制提示**：图片以 100% 缩放展示，用鼠标跟随旁白位置；提到"暗带上缘 y₀"时指向图中对应节点。

### S5 现场运行与复现自检｜1:02–1:24（22 s）

- **录屏画面**：终端全屏。先粘贴第 1 条命令，输出"检测成功 / 中心坐标 / 面积 / 外接矩形"；紧接着粘贴第 2 条命令，输出滚动到"自检结论：全部通过"；最后执行第 3 条命令，终端无任何输出。
- **终端命令**（逐条执行，命令之间停 1 秒）：

  ```powershell
  .venv\Scripts\python.exe demo\run_single_image_detection.py
  .venv\Scripts\python.exe demo\run_m63_final_visualization.py
  git status --short --untracked-files=no
  ```

- **需要展示的文件**（命令跑完后切出 2 秒）：
  - `results/EXP-001-IMAGE-001_overlay.png`（单图检测结果）
  - `results/EXP-EXT-LAB67-V1_fig_A_y0_timeseries.png` 等三张图（刚被重新生成）
- **旁白**：现在现场运行。第一条命令检测单张图片，零点四秒完成；第二条命令只读封板轨迹，重新生成三张成果图，脚本自动跑完四十一项自检，全部通过；最后仓库状态没有任何改动。
- **录制提示**：
  - 真实耗时刻度：命令 1 约 **0.4–0.5 s**，命令 2 约 **2.9–3.1 s**，命令 3 约 0.3 s（本机多次实测）。剪辑时若担心冷场，可 2× 加速等待段并加字幕"实际耗时约 3 秒"。
  - 运行命令 1、2 会**重写** `results/` 中的同名成果文件；本机 2026-09-26 实测重写后 SHA-256 与仓库版本**逐字节一致**，已跟踪文件零改动，因此该镜头可以安全重复录制（哈希对照见附录 B）。
  - 命令 1 需要本机的 `data/raw/EXP-001-IMAGE-001.jpg`：它是自采素材，**不在 Git 仓库中**，发布 ZIP 也不包含。本机当前存在该文件；若换机器录制且没有该文件，请只演示命令 2 与命令 3（命令 2 只需要仓库内的封板轨迹 CSV）。
  - 命令 3 写成 `git status --short --untracked-files=no`：本脚本自身是未跟踪文件，直接敲 `git status --short` 会多出 `?? demo_video_script.md` 一行，容易让观众误以为项目被改动。
  - 中文输出若出现乱码，退出录制后重新 `chcp 65001` 再录。

### S6 测量结果与三方对照｜1:24–1:48（24 s）

- **录屏画面**：
  - 0:00–0:12 图 A 全屏（`results/EXP-EXT-LAB67-V1_fig_A_y0_timeseries.png`）：先展示 160 个原始采样点与 10 峰 / 10 谷标记，再指认单处峰峰区间与标注数值。
  - 0:12–0:24 图 B 全屏（`results/EXP-EXT-LAB67-V1_fig_B_frequency_comparison.png`）：依次指认"实验测量 1.843003 Hz"、"标准模型参考 1.862134625 Hz"、"FFT 主峰 1.875 Hz"三个标注。
- **终端命令**：无。
- **需要展示的文件**：`results/EXP-EXT-LAB67-V1_fig_A_y0_timeseries.png`、`results/EXP-EXT-LAB67-V1_fig_B_frequency_comparison.png`（可与 PPT 第 5、8 页交替）。
- **旁白**：图 A 是原始位置序列：160 个采样点、10 峰 10 谷，未平滑、未插值，峰峰间隔给出周期 0.542593 秒。图 B 做三方对照：时域 1.843 赫兹、FFT 主峰 1.875 赫兹、标准模型参考 1.862 赫兹，相差约 1%。
- **录制提示**：说到"约 1%"时必须与"条件性模型参考、差异未归因"同时表达（见第 8 节），不得表述为算法精度。

### S7 追踪叠加可视化｜1:48–2:00（12 s）

- **录屏画面**：播放 `results/EXP-004-DYNAMIC-001_overlay.mp4`：
  - 0:00–0:06：正常速度播放 **00:00–00:06** 片段；
  - 0:06–0:12：播放 **00:13.5–00:16.0**（约 2.5 s），0.5× 慢放，画面停在转向点帧。
  画面左上角可见黄色 `frame=... t=... s` 与绿色 `center=(...) px` 叠加信息，绿色矩形框为检测目标。
- **终端命令**：无（可直接用系统播放器播放；如需在终端打开：`start results\EXP-004-DYNAMIC-001_overlay.mp4`）。
- **需要展示的文件**：`results/EXP-004-DYNAMIC-001_overlay.mp4`。
- **旁白**：这是自采的追踪叠加视频，左上角显示帧号、时间和像素中心，绿框是检测结果；第十三点九秒是本段转向点。
- **录制提示**：该片段属于本项目自采数据，可自由展示；播放器隐藏进度条与控制栏以获得干净画面。若需替换为 `results/EXP-003-STATIC-001_overlay.mp4`，请同步改旁白中的时间点。

### S8 GitHub 仓库展示｜2:00–2:14（14 s）

- **录屏画面**：浏览器打开 <https://github.com/yangzi0808/VisionMotion>。按顺序展示：仓库标题与简介 → 目录树（`src/`、`demo/`、`docs/`、`results/`）→ README 的"主要结果"表格 → `docs/` 目录下的最终报告、PPT、数据来源说明。
- **终端命令**：无（可先在终端执行 `git log --oneline -5` 作为"提交历史"补镜，再切浏览器）。
- **需要展示的文件**：README.md、`docs/M6.3_FINAL_REPORT.md`、`docs/VisionMotion_Final_Presentation.pptx`、`docs/ATTRIBUTION.md`。
- **旁白**：代码、文档与成果图都已发布在 GitHub，采用 MIT 许可。README 可复现完整流程，docs 下有最终报告、答辩 PPT 与数据来源说明。
- **录制提示**：**录制前请确认 GitHub 页面状态**（当前远端仍停留在 `5a81b67`，本地领先 1 个提交，详见附录 C）；浏览器提前打开好标签页，避免现场加载等待。

### S9 结论、局限与收尾｜2:14–2:30（16 s）

- **录屏画面**：PPT 第 10 页全屏。先展示"已经完成"清单，再下移展示"当前限制"，最后停在页面底部的报告路径与仓库地址。
- **终端命令**：无。
- **需要展示的文件**：`docs/VisionMotion_Final_Presentation.pptx` 第 10 页。
- **旁白**：当前只用单一视频、单一质量，还没有动态像素标定，位置仍以像素表达，百分之一的差异还不能归因。下一步是自采多组数据完成标定。谢谢观看。
- **录制提示**：结尾定格 2 秒再停止录制，便于后期加淡出与仓库地址字幕。

---

## 5. 终端命令合集

### 5.1 开录前（不录入成片）

```powershell
chcp 65001
Clear-Host
Set-Location 'C:\Users\杨千贤\Desktop\VisionMotion'
git status --short --untracked-files=no   # 预期：无输出（已跟踪文件零改动）
git log --oneline -1          # 预期：3e41ff9 docs: update license reference
```

### 5.2 正式演示命令与预期输出

| # | 命令 | 实测耗时 | 关键输出 | 产出文件 |
| --- | --- | --- | --- | --- |
| 1 | `.venv\Scripts\python.exe demo\run_single_image_detection.py` | 0.42 s | `检测成功`；中心坐标 `x = 512.7 px`、`y = 964.5 px`；面积 `1278.5 px`；外接矩形 `x=494 y=940 w=37 h=51` | `results/EXP-001-IMAGE-001_overlay.png`、`results/EXP-001-IMAGE-001_mask.png` |
| 2 | `.venv\Scripts\python.exe demo\run_m63_final_visualization.py` | 2.92 s | 41 项自动自检逐条打印，末行 `自检结论：全部通过`；三张图 3900×1980 / 3900×1980 / 4800×2700（dpi 300） | 三张 `results/EXP-EXT-LAB67-V1_fig_*.png` |
| 3 | `git status --short --untracked-files=no` | 0.3 s | 无输出（已跟踪文件零改动） | — |

> 关于命令 1：`demo\run_single_image_detection.py` 读取 `data/raw/EXP-001-IMAGE-001.jpg`。该图片是项目自采素材，
> **不在 Git 仓库中**（`git ls-files data/` 只有 `.gitkeep`），发布 ZIP 也不包含；本机当前存在该文件，可直接运行。
> 若换机器录制，请先确认文件存在，否则跳过命令 1，只演示命令 2 与命令 3。
>
> 关于命令 3：本脚本 `demo_video_script.md` 是未跟踪文件，直接执行 `git status --short` 会输出
> `?? demo_video_script.md`。加 `--untracked-files=no` 后，只有**已跟踪文件**被改动才会有输出，因此空白画面才真正表示"零改动"。

### 5.3 可选补充命令

| # | 命令 | 实测耗时 | 说明 |
| --- | --- | --- | --- |
| 4 | `.venv\Scripts\python.exe demo\run_m53_plot.py` | 0.94 s | 只读 `results/EXP-004-DYNAMIC-001_ds.csv`（1766 行），生成 1800×900 `results/EXP-004-DYNAMIC-001_displacement.png`，并打印 CSV SHA-256 前后一致：`704D6C64…4163` |
| 5 | `.venv\Scripts\python.exe demo\run_video_tracking.py` | 未实测 | 需要本地 `data/raw/EXP-002-VIDEO-001.mp4`（952 帧，15.83 s）；预计数十秒，请提前试跑后再决定是否入镜 |
| 6 | `.venv\Scripts\python.exe demo\pick_calibration_points.py` | 交互式 | 需鼠标点选 P1/P2 两个刻度点后按 Enter 确认，**不建议现场录制**，可改为播放预录屏 |

### 5.4 演示脚本的可重复性说明

命令 1、2、4 会重写 `results/` 中同名文件。本机实测（2026-09-26）重写后 SHA-256 与仓库版本**逐字节一致**（六个文件全部比对通过，见附录 B），故已跟踪文件保持干净；若仍不放心，可按第 2 节第 6 步先备份 `results/`。注意检查命令要写成 `git status --short --untracked-files=no`，否则未跟踪的 `demo_video_script.md` 会出现在输出里。

---

## 6. 旁白全文（提词器版）

> 朗读基准语速约 235 字/分钟（可读区间 210–250）；全部为口语化短句，避免长定语。

**[S1 0:00]** 大家好，我是深圳大学光电信息科学与工程专业的本科生。这里展示我的项目 VisionMotion。

**[S2 0:10]** 项目想回答一个朴素的问题：普通摄像头拍的视频，能否测出机械目标的振动周期和频率。链路是视频、位置序列、周期、频率、FFT 交叉验证，最后与物理模型对照。

**[S3 0:28]** 数据必须说清楚：正式分析用的是外部公开教学数据 ComPADRE Lab67 Video 1，不是自主采集，许可为 CC BY-NC-SA 3.0；分析区间第 76 到 235 帧，共 160 帧。

**[S4 0:46]** 方法不使用深度学习，完全可解释：固定 ROI 内做灰度阈值、暗连通域与行宽带筛选，取暗带上缘 y₀ 作为位置特征，160 帧全部检出，无缺口。

**[S5 1:02]** 现在现场运行。第一条命令检测单张图片，零点四秒完成；第二条命令只读封板轨迹，重新生成三张成果图，脚本自动跑完四十一项自检，全部通过；最后仓库状态没有任何改动。

**[S6 1:24]** 图 A 是原始位置序列：160 个采样点、10 峰 10 谷，未平滑、未插值，峰峰间隔给出周期 0.542593 秒。图 B 做三方对照：时域 1.843 赫兹、FFT 主峰 1.875 赫兹、标准模型参考 1.862 赫兹，相差约 1%。

**[S7 1:48]** 这是自采的追踪叠加视频，左上角显示帧号、时间和像素中心，绿框是检测结果；第十三点九秒是本段转向点。

**[S8 2:00]** 代码、文档与成果图都已发布在 GitHub，采用 MIT 许可。README 可复现完整流程，docs 下有最终报告、答辩 PPT 与数据来源说明。

**[S9 2:14]** 当前只用单一视频、单一质量，还没有动态像素标定，位置仍以像素表达，百分之一的差异还不能归因。下一步是自采多组数据完成标定。谢谢观看。

---

## 7. 后期剪辑要点

1. 全程加中文字幕；专有名词保留英文原文（ROI、FFT、SHM、px）。
2. 关键数字出现时加 0.5 s 的圆形高亮或 110% 局部推近：`0.542593 s`、`1.843003 Hz`、`1.875 Hz`、`1.862134625 Hz`、`160/160`、`约 1.0%`。
3. 终端等待段可 2× 加速，并加字幕"实际耗时约 3 秒"。
4. 转场只用硬切或淡入淡出（0.3 s），不使用花哨特效，符合实验报告风格。
5. 音量：旁白约 −16 LUFS；BGM 若有需低于 −30 dB 或不使用；终端键盘声保留但不放大。
6. 片尾定格 2 s，叠加仓库地址、MIT 许可与"外部数据 CC BY-NC-SA 3.0"字样。
7. 导出后自检：总时长 2:25–2:35、音画同步、字幕无错别字、无通知弹窗入镜。

---

## 8. 数据合规与表述红线

| 项 | 要求 |
| --- | --- |
| 数据性质 | 必须说明正式分析数据为**外部公开教育数据**（ComPADRE / OSP Entry ID 16081，作者 Kathleen Koenig），不是自主采集 |
| 署名与许可 | 片中需出现 **CC BY-NC-SA 3.0** 字样；完整署名见 `docs/ATTRIBUTION.md` |
| 外部视频画面 | **不要在成片中连续播放第三方视频画面**（避免再分发风险）；如需单帧引用，须叠加来源与许可 |
| 自采数据 | `data/raw/EXP-00X-*` 为项目自采数据，其叠加视频可自由展示 |
| 单位表述 | 位置为 **y₀（px，像素）**；本阶段**没有**为该外部视频建立有效动态 px/mm 标定，**不得口播毫米位移或振幅** |
| "约 1%" | 指实验频率与标准质量—弹簧模型参考值之差，**不是算法精度或测量精度指标**；必须同时说明该模型为项目采用的标准 SHM 条件性参考、差异物理原因未验证 |
| 时间分辨率 | 30 fps → 单帧 0.0333 s；正式记录长度 5.3333 s；FFT 分辨率 df = 0.1875 Hz |
| 代码许可 | MIT License（Copyright (c) 2026 yangzi0808），见仓库根目录 `LICENSE` |

### 本片禁止出现的表述

- "测量精度达到 1%" / "误差只有 1%"（应说：与标准模型参考值相差约 1.0%）。
- "毫米级位移测量"（应说：本阶段位置以像素 y₀ 表达）。
- "我们自己拍摄的实验视频"（指代外部 Lab67 数据时）。
- 任何未经仓库文件支持的荣誉、成绩或第三方背书。

---

## 9. 风险与备用方案

| 风险 | 备用方案 |
| --- | --- |
| 现场命令报错 | 提前录好一段成功运行的屏幕录像，剪辑时替换该镜头 |
| 中文输出乱码 | 重新 `chcp 65001` 后重录；或改用 Windows Terminal / PowerShell 7 |
| GitHub 页面加载慢或内容与本地不一致 | 提前打开标签页；必要时改为展示本地 `README.md` 与 `docs/`（见附录 C） |
| 旁白超时 | 删除 S2 的链路复述或 S6 的 FFT 细节；使用附录 A 的 2:00 精简版 |
| 叠加视频播放卡顿 | 先用播放器预加载；或改用 `results/EXP-003-STATIC-001_overlay.mp4` 片段并同步改旁白 |
| 通知弹窗入镜 | 开启专注助手 + 断网录屏（GitHub 镜头单独联网录制） |

---

## 10. 录制后检查清单

- [ ] 成片总时长 2:25–2:35，镜头与时间轴总览一致
- [ ] S3 出现数据来源与 CC BY-NC-SA 3.0 署名
- [ ] S5 终端输出完整包含"自检结论：全部通过"与空白 `git status --short --untracked-files=no` 画面
- [ ] S6 三个频率数值与读法无误（1.843003 / 1.875 / 1.862134625 Hz）
- [ ] S7 叠加视频画面中帧号、时间、像素中心清晰可读
- [ ] S8 GitHub 页面为最新状态或已按附录 C 处理
- [ ] 全片无真实姓名（`docs/AUTHOR_PROFILE.md` 姓名栏为"待填写"）
- [ ] 无毫米位移 / 精度类越界表述
- [ ] 录制结束后 `git status --short --untracked-files=no` 仍为空（已跟踪文件零改动）

---

## 附录 A　时长调整方案

| 版本 | 总时长 | 调整方式 |
| --- | --- | --- |
| 精简版 | ≈ 2:00 | 删除 S4；S6 压缩到 18 s；S7 只保留 6 s 正常速度片段 |
| 标准版 | 2:30 | 按本文档执行 |
| 扩展版 | ≈ 3:00 | S5 追加命令 4（`run_m53_plot.py`）；S7 追加 `EXP-003-STATIC-001_overlay.mp4` 片段；S8 追加 `git log --oneline -5` 提交历史镜头 |

---

## 附录 B　数值来源对照表

| 片中数值 | 来源文件 |
| --- | --- |
| T_exp = 0.542593 s、f_exp = 1.843003 Hz | README §6；`docs/M6.3_FINAL_REPORT.md` §5.3；PPT 第 5、6 页 |
| FFT 主峰 1.875 Hz、df = 0.1875 Hz | README §6；报告 §6；PPT 第 6 页 |
| m = 0.0515 kg、k = 7.05 N/m、f_theory = 1.862134625 Hz | README §6；报告 §7；PPT 第 7 页 |
| 差异 +1.0381%（周期）/ −1.0274%（频率） | README §6；报告 §8；PPT 第 8 页 |
| 1920 × 1080、30 fps、236 帧、约 7.87 s、frame 76–235（160 帧，5.3333 s） | README §4；报告 §2；PPT 第 2、3 页 |
| 检出 160 / 160 | README §5；报告 §3.3；PPT 第 4 页 |
| ROI x ∈ [690, 780)、y ∈ [380, 760)、gray < 120、面积 ≥ 150 px、行跨度 ≥ 35 px | README §5；报告 §3.2；PPT 第 4 页 |
| y₀ = 508–622 px、10 峰 + 10 谷 | PPT 第 5 页；报告 §4、§5.2 |
| 转向点 frame 491 / 602 / 838 / 1063 / 1254 / 1513 | `demo/run_m53_plot.py` 终端输出（M5.2 冻结值） |
| 叠加画面 `frame=838 t=13.938 s`、`center=(1099.2, 216.3) px` | `results/EXP-004-DYNAMIC-001_overlay.mp4` 抽帧实测 |
| 三张成果图尺寸 3900×1980 / 3900×1980 / 4800×2700（dpi 300） | `demo/run_m63_final_visualization.py` 自检输出 |
| 命令耗时 0.42 s / 2.92 s / 0.94 s | 本机实测（2026-09-26，venv：Python 3.14.1 + opencv 5.0.0 + numpy 2.5.3 + matplotlib 3.11.2） |
| 重写文件哈希与仓库一致 | 实测：mask `002BC9B7…`、overlay `05DA89A5…`、displacement `8E908997…`、fig A `909A00E9…`、fig B `CF385D7A…`、fig C `A31478F6…` |

---

## 附录 C　录制前需要确认的事项（本次未修改任何文件）

1. **GitHub 页面落后本地 1 个提交**：远端 `origin/master` 为 `5a81b67`，本地 `master` 为 `3e41ff9 docs: update license reference`。该提交只改了 README 第 15 节的许可表述，**尚未推送**。若 S8 镜头会拍到 README 的 License 段落，建议先推送再录制（推送属于仓库写操作，本脚本不代为执行）。
2. **`docs/ATTRIBUTION.md` 与 README/LICENSE 的许可表述不一致**：`docs/ATTRIBUTION.md` 末条仍写"本项目自身的代码许可尚未声明"，而 README §15 与根目录 `LICENSE` 已声明 MIT。若 S8 会展开 `docs/ATTRIBUTION.md`，建议先统一文案，避免同一镜头里出现两种说法。
3. 本文档 `demo_video_script.md` 为新增文件，未提交、未纳入此前的发布 ZIP（`VisionMotion_v1.0.zip`）；如需随包分发请重新打包。
