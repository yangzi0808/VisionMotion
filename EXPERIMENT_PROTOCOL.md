# EXPERIMENT_PROTOCOL — 实验记录模板

每次拍摄、每组数据都要单独填写一份记录，做到"任何一个结果都能回溯到当时的拍摄条件"。
填写时直接复制下面的【记录模板】并补全内容。文件命名建议：`YYYYMMDD_EXP-编号`，
例如视频 `data/raw/20260922_EXP-001.mp4`、记录 `EXP-001`。

## 填写要点

- 一次实验对应一个视频文件、一种被测目标状态；只要改变任一条件（距离、光照、载荷、帧率），就新建一条记录。
- 摄像头参数尽量写全：设备型号、分辨率、帧率、曝光与对焦方式、是否开防抖。
- 拍摄距离要写清测量方式（从哪里量到哪里，单位 cm 或 m）。
- 标定信息必须记录参考物的实际长度和它在画面中的像素长度，两者相除得到 mm/像素。
- 载荷/运动条件要写清初始位移、释放或驱动方式、是否自由衰减、激励频率（若有）。
- 原始数据文件写相对路径（如 `data/raw/20260922_EXP-001.mp4`），不要只写文件名。
- 处理结果写清用了哪个脚本、输出文件在哪里，以及关键数值（周期、频率、幅值、判断结论）。
- 备注中记录任何异常：过曝、对焦不清、有人走动、手机自动降帧、支架轻微晃动等。

## 记录模板

```text
### EXP-编号

- 实验编号：
- 日期：
- 设备：
- 摄像头参数：
- 被测物：
- 拍摄距离：
- 实验条件：
- 载荷/运动条件：
- 标定信息：
- 原始数据文件：
- 处理结果：
- 备注：
```

## 各字段填写说明

| 字段 | 应填写的内容 |
| --- | --- |
| 实验编号 | 统一编号，如 EXP-001，与视频文件名保持一致 |
| 日期 | 拍摄日期 YYYY-MM-DD，最好带开始时间 HH:MM |
| 设备 | 手机/相机型号、固定方式（三脚架、手机夹等）、所用电脑 |
| 摄像头参数 | 分辨率、帧率、曝光模式、对焦方式、是否开启防抖、拍摄软件 |
| 被测物 | 被测目标名称、尺寸、材料、安装方式 |
| 拍摄距离 | 镜头到目标运动平面的距离，注明单位与测量方式 |
| 实验条件 | 光照（自然光/室内灯）、背景、室内外、有无风、机位是否固定 |
| 载荷/运动条件 | 初始位移、释放方式或驱动方式、激励频率、是否自由衰减、测量方向 |
| 标定信息 | 参考物与其实长、画面中像素长度、标定系数 mm/像素 |
| 原始数据文件 | 视频文件的相对路径，必要时附上标定截图路径 |
| 处理结果 | 处理脚本名、输出文件路径、周期 T、频率 f、幅值、状态判断 |
| 备注 | 异常情况、拍摄时的手工调整、其他需要提醒自己或他人的信息 |

## 填写示例（示范格式，非真实实验数据）

```text
### EXP-001

- 实验编号：EXP-001
- 日期：2026-09-22 15:30
- 设备：手机（型号示例） + 桌面三脚架；处理用笔记本电脑（Windows）
- 摄像头参数：1920×1080，60 fps，手动曝光与手动对焦，关闭防抖，手机自带相机 App
- 被测物：钢尺末端悬臂梁（长约 200 mm），末端贴直径 20 mm 红色圆形标记
- 拍摄距离：镜头到悬臂梁运动平面约 80 cm（卷尺测量）
- 实验条件：室内，关闭顶灯，用一盏台灯侧面照明；白墙背景；无风；三脚架固定
- 载荷/运动条件：末端初始位移约 10 mm，自由释放后自由衰减振动，测量方向为竖直方向
- 标定信息：画面中 100 mm 直尺对应 430 像素，标定系数 ≈ 0.233 mm/像素
- 原始数据文件：data/raw/20260922_EXP-001.mp4
- 处理结果：待处理（脚本：未实现；预期输出 results/EXP-001_displacement.png）
- 备注：拍摄时相机自动降帧到 60 fps 以下未出现；目标标记对焦清晰
```
