# PROJECT_SPEC — VisionMotion 项目说明书

本文档定义 VisionMotion 第一版要做什么、不做什么、怎么做。
开始写代码前先看这里；如果需求有变化，**先改本文档，再改代码**。

## 1. 项目目标

**总目标**

用低成本的非接触方式测量简单机械目标的位移与振动：用普通手机/电脑摄像头拍摄目标运动视频，
用计算机视觉方法把视频中的像素位置变化换算成实际位移，并进一步得到振动周期、频率和状态判断结果。

**第一版具体目标**

1. 能从一段事先拍好的视频中稳定提取目标标记的中心位置。
2. 能把像素坐标通过标定换算成毫米（mm）。
3. 能输出位移-时间曲线，并读出振动周期与频率。
4. 能用简单阈值给出"正常 / 异常"这类状态判断。
5. 代码简单、步骤清晰，作者能逐行解释每个环节在做什么。

**非目标（本阶段不追求）**

- 不追求计量级精度，不追求亚像素、微米级测量。
- 不做实时在线处理，不做三维、多相机测量。
- 不追求对任意目标、任意背景的通用识别能力，只针对事先设计好的目标标记。

## 2. 输入

| 输入 | 说明 | 存放位置 |
| --- | --- | --- |
| 目标运动视频 | 手机或电脑摄像头拍摄的机械目标运动视频，常见格式（mp4 / avi / mov） | `data/raw/` |
| 拍摄参数 | 分辨率、帧率、曝光与对焦方式、是否开启防抖 | 记录在 `EXPERIMENT_PROTOCOL.md` |
| 标定信息 | 画面中已知实际长度的参考物（如直尺）及其画面中的像素长度 | `data/raw/` + 实验记录 |
| 目标标记说明 | 标记的形状、颜色、实际尺寸 | 实验记录 |
| 实验记录 | 每个视频对应的实验条件与载荷条件 | `EXPERIMENT_PROTOCOL.md` |

**对视频的基本要求（第一版的前提假设）**

- 摄像头固定（三脚架或固定支架），拍摄过程中机位、焦距、拍摄距离不变。
- 目标在画面中部运动，运动平面与镜头光轴尽量垂直。
- 光照稳定、无剧烈明暗变化，目标标记与背景的颜色/亮度对比明显。
- 参考物与目标运动平面尽量在同一平面内。
- 记录帧率与分辨率；目标运动不要快到产生明显运动模糊。

## 3. 输出

1. **中心位置序列**：每帧的目标中心像素坐标 (x, y)。
2. **标定系数**：mm/像素（同时给出其倒数，便于检查是否算反）。
3. **位移序列**：以初始位置为参考的位移 d(t)，单位 mm。
4. **数据文件**：位移-时间数据（如 CSV：时间、像素坐标、位移）。
5. **曲线图**：位移-时间曲线图片（PNG），保存到 `results/`。
6. **振动参数**：周期 T（s）与频率 f = 1/T（Hz），并说明由几个周期平均得到。
7. **状态判断**：按设定阈值给出结论（如 正常 / 异常），并写明所用阈值与判断依据。
8. **结果说明**：一段简短文字，把数值、图与结论串起来，可写入实验记录或报告。

第一版只输出**单方向（主运动方向）**的位移结果；另一方向只作为参考记录，不做分析。

## 4. 第一版功能与完成标准

| 编号 | 功能 | 完成标准 |
| --- | --- | --- |
| 1 | 视频读取 | 能打开指定视频，读出总帧数、帧率、分辨率，并能显示任意一帧 |
| 2 | 目标标记检测 | 对测试视频能在每帧中定位目标标记（基于颜色/亮度阈值 + 轮廓检测），并能输出二值掩膜图便于检查 |
| 3 | 目标中心位置提取 | 逐帧给出目标中心像素坐标，并保存为序列 |
| 4 | 像素坐标到实际长度标定 | 输入参考物像素长度与实际长度，算出 mm/像素，并用另一个已知长度做校验 |
| 5 | 位移计算 | 以起始参考位置为原点，输出每帧位移（mm） |
| 6 | 位移-时间曲线 | 生成位移-时间曲线图并保存 |
| 7 | 振动周期检测 | 用简单峰值检测找出位移曲线的峰（或谷），给出周期估计值 |
| 8 | 振动频率计算 | 由周期给出频率，并对多个周期取平均以减小误差 |
| 9 | 阈值状态判断 | 用 1~2 个简单阈值（如位移幅值、频率偏差）给出状态结论与依据 |
| 10 | 结果可视化 | 至少输出位移曲线图 + 关键数值 + 判断结论，保存到 `results/` |

每项功能都要求：**输入明确、输出明确、出错时有能看懂的错误提示**。

## 5. 明确不做的内容

**第一版禁止使用的技术**

原因：这些方法对本阶段的精度提升有限，但会显著增加理解与调试成本。

- YOLO
- PyTorch
- TensorFlow
- 深度学习 / 神经网络
- Kalman 滤波
- DIC 完整算法
- 有限元、COMSOL
- 复杂振动理论
- 复杂信号处理（多级滤波、时频分析等）
- 复杂优化算法
- 任何没有明确必要性的第三方框架

**第一版不做的事情**

- 实时视频流采集与在线处理（只处理已经拍好的视频文件）。
- 多相机、三维重建、二维全场测量。
- 对任意目标、任意背景的自动识别（只识别事先设计好的目标标记）。
- 相机畸变校正、亚像素定位、运动模糊复原。
- 图形界面（GUI）：前期用命令行 + 图片输出即可。

## 6. 技术路线

处理流程（每一步只做一件事，前一步的输出就是后一步的输入）：

1. **采集**：固定机位拍摄目标运动视频，画面中放置已知尺寸参考物，同步填写实验记录。
2. **读取**：用 OpenCV 逐帧读取视频，先检查帧率与帧数是否符合预期。
3. **预处理**：转灰度或转 HSV，必要时做模糊去噪、形态学处理，为阈值分割做准备。
4. **目标标记检测**：用颜色/亮度阈值分割出标记区域（得到二值图），再做轮廓检测，
   保留面积与位置符合条件的目标轮廓。
5. **中心提取**：用轮廓矩计算质心 (cx, cy)，作为该帧的目标中心，逐帧记录。
6. **标定**：k = 参考物实际长度 ÷ 参考物像素长度（mm/像素），并用另一段已知长度验证。
7. **位移计算**：以起始若干帧的平均位置为参考点，d(t) = (p(t) − p₀) × k，只取主运动方向分量。
8. **曲线绘制**：用 Matplotlib 画位移-时间曲线，先检查有没有跳点、丢帧、异常抖动。
9. **峰值检测**：对去均值后的位移序列做简单峰值检测（幅值阈值 + 最小间隔），得到峰值位置序列。
10. **周期与频率**：相邻峰值时间间隔取平均得到周期 T，f = 1/T；峰数太少就只报告趋势，不下结论。
11. **状态判断**：设定简单阈值（例如幅值是否超限、频率是否偏离预期、是否有异常突跳），输出结论与依据。
12. **结果输出与记录**：把 CSV、图片、关键数值保存到 `results/`，并把结论写回实验记录。

**关键假设与误差来源（写报告时必须说明）**

- 假设目标在垂直于光轴的平面内运动，平面倾斜会带来比例误差。
- 假设拍摄距离与焦距在拍摄过程中不变。
- 未做镜头畸变校正：画面边缘误差更大，所以目标尽量在画面中心附近运动。
- 光照变化、反光、目标旋转、运动模糊都会影响阈值分割与质心位置。
- 标定精度直接决定位移精度：标定尺要放得够长、边界要读准、并与目标同平面。

**降低误差的简单办法**：固定曝光与对焦、增大目标标记、提高帧率、增强目标与背景对比、
在视场允许范围内缩短拍摄距离、每次实验都重新标定并记录。

## 7. 未来可扩展方向

第一版跑通并验证后，再按需要逐步加入，每次只加一项，加完先验证再继续：

- **亚像素中心定位**：灰度重心或轮廓拟合，提高位移分辨率。
- **相机畸变校正**：用标定板校正镜头畸变，改善画面边缘精度。
- **视频抗抖动**：用背景特征点做简单稳定化，减小机位微小晃动的影响。
- **频谱分析**：用 FFT 求主频，作为峰值检测的交叉验证。
- **二维轨迹**：同时分析 x、y 两个方向，观察摆动或椭圆轨迹。
- **自动标定**：自动识别已知尺寸的标定物。
- **多目标测量**：同时测量多个点，观察相对位移。
- **对比验证**：与加速度计、激光位移计或千分表的测量结果对比，评估误差。
- **批量处理与简易界面**：一次处理多个视频，或用图形界面选择参数。
- **拍摄条件对比实验**：不同手机、不同帧率、不同距离下的精度对比。

长期可选、且只有第一版已跑通并确有精度需求时才考虑：DIC 类全场测量方法、基于学习的特征跟踪等。
