# CHANGELOG — 版本更新记录

本文件记录项目每一步的变化。规则很简单：

- 每次改代码或改文档都在"未发布"下面加一条记录。
- 记录三件事：**做了什么、为什么做、影响了哪些文件**。
- 版本号规则：开发阶段用 `v0.x.y`；第一版功能全部完成并验证通过后发布 `v1.0.0`。
- 发布一个版本时，把"未发布"里的内容移到新的版本号下面，并写上日期。

## 未发布

### 新增

- 建立项目目录骨架：`src/`、`data/raw/`、`data/processed/`、`results/`、`tests/`、`docs/`、`demo/`
- 添加 `README.md`（项目简介与当前状态）、`PROJECT_SPEC.md`（项目说明书）、`EXPERIMENT_PROTOCOL.md`（实验记录模板）
- 添加 `requirements.txt`，列出第一阶段依赖：OpenCV、NumPy、Matplotlib
- 添加 `.gitignore`，排除虚拟环境、Python 缓存、临时文件与大型视频
- 原因：先固定项目范围与记录方式，再开始写代码，避免边写边改需求
- **M1 开发环境**：创建项目专用虚拟环境 `.venv`（Python 3.14.1），只安装本阶段需要的库
- **M1 开发环境**：在 `.venv` 中安装并验证 `opencv-python` 5.0.0.93（`import cv2` 成功）
- **M1 开发环境**：在 `.venv` 中安装并验证 `numpy` 2.5.3（`import numpy` 成功）
- **M1 开发环境**：在 `.venv` 中安装并验证 `matplotlib` 3.11.2（`import matplotlib` 成功）
- 原因（M1）：只建立并验证开发环境，不写任何业务代码

### 变更

- 无

### 修复

- 无

### 说明

- 当前只有文档、配置与开发环境，`src/` 下尚无任何业务代码

## [v0.6.1] - 2026-09-25

### 新增

- **M6.3 周期 / 频率测量（外部公开视频）**：基于 M6.2-2C 封板轨迹 `results/EXP-EXT-LAB67-V1_trajectory.csv`（frame 76–235，160 帧，5.3333 s）完成时域峰谷与周期测量——10 个局部极大 + 10 个局部极小，峰—峰 9 段均值 **T_exp = 0.542593 s**、**f_exp = 1.843003 Hz**；峰—峰 / 谷—谷 / 半周期三种时域方法互差 ≤ 0.09%
- **FFT 频域交叉验证**：对原始 y₀(t)（仅减均值，未加窗、未去趋势、未重采样）做 rFFT，主峰 **1.875 Hz**，频率分辨率 **df = 0.1875 Hz**；FFT 仅作独立交叉验证，不替代时域测量
- **M6.3-2A 外部实验参数核验**：核对 `Equipment values - IV is Amount of Mass on Srping.pdf`，确认 k = 7.05 N/m（±0.045 N/m）、Lab67 Video 1 → mass on spring = 0.0515 kg（±0.00005 kg）；资料未给出弹簧自身质量、未提及有效质量修正、未给出静态伸长量，亦未明文写出周期公式
- **M6.3-2B 理论—实验交叉验证**：采用**项目采用**的标准质量—弹簧 SHM 模型 T = 2π√(m/k)（非外部资料明文），得 T_theory = 0.537018101 s、f_theory = 1.862134625 Hz；实验与参考值差异 ΔT = +0.005574899 s（+1.0381%）、Δf = −0.019131625 Hz（−1.0274%），统一概括为约 1.0%
- **M6.3-2C / 2C-R 差异边界分析**：参数矩形 f ∈ [1.855281725, 1.868975645] Hz（实验值位于其下界之外）；三层不确定度分离记录——A 参数传播尺度 ±0.3228%（±0.006011 Hz）、B 实验统计 SE（SE_T ≈ 0.0038 s、SE_f ≈ 0.0129 Hz）、C 保守采样定位上界（±0.0333 s、±0.1132 Hz）；结论为"差异超过 A、未超过 C"；含四角点参数边界表的算术纠错（角点 4）
- **M6.3-3B 最终成果可视化**：新增 `src/m63_final_visualization.py` 与 `demo/run_m63_final_visualization.py`，按 M6.3-3A 封板设计稿生成三张 300 dpi 最终图 `results/EXP-EXT-LAB67-V1_fig_A_y0_timeseries.png`、`..._fig_B_frequency_comparison.png`、`..._fig_C_pipeline.png`，含 41 项自动自检与人工视觉检查
- **M6.3-3C 最终项目报告**：新增 `docs/M6.3_FINAL_REPORT.md`（摘要 + 14 章 + 附录 A：数据源、视觉测量方法、y₀(t)、峰谷与周期、FFT、**标准 SHM** 条件性参考、实验—参考差异、三层不确定度与证据边界、未验证因素、技术链、项目限制、结论、参考资料）

### 变更

- `README.md`：从 M3 阶段快照更新到 M6.3 最终状态（在 M7.2 完成）
- 新增 `docs/ATTRIBUTION.md`：第三方公开数据的来源、作者、许可与哈希记录

### 修复

- 无

### 说明

- 外部数据性质：ComPADRE / OSP Lab67 Video 1（条目 ID 16081，作者 / 权益持有人 Kathleen Koenig，许可 CC BY-NC-SA 3.0），**不是本项目自主采集**；视频 SHA-256 见 `docs/ATTRIBUTION.md`
- 本阶段**未为该外部公开视频建立并应用有效的动态 px/mm 标定**，正式位置结果以像素 y₀ 表达，未进行毫米位移换算
- 未做阻尼、弹簧质量、有效质量等参数反演；实验与参考值约 1% 差异的**具体物理原因未验证**

## [v0.6.0] - 2026-09-25

### 新增

- **M6.2-1 外部数据登记与视觉测量可行性审查**：登记外部公开教育视频数据集 ComPADRE / OSP Lab67 归档（含 Video 1–4、2 份 PDF、4 个 Tracker `.trk`、HTML 信息页），完成 SHA-256 三方比对（归档 / 镜像 / 解出副本一致）、许可证与权益记录、代表帧视觉审查、标尺与 px/mm 候选测量（**仅记录，未做正式标定**）、目标候选与追踪特征分析，并输出 A/B/C 可行性判定
- **M6.2-2A / 2B 深灰目标受控诊断与正式特征选择**：对“暗带（悬挂组件夹持块）”做全片 236 帧受控诊断（检出率、最大连续丢失、bbox/面积范围、高速模糊窗口、误锁检查、f0–73 身份一致性、f74–75 条件放宽测试），冻结正式视觉方案并确定正式位置特征为**暗带上缘 y₀**
- **M6.2-2C 正式外部轨迹实现**：新增 `src/external_oscillation_tracker.py` 与 `demo/run_m62_external_tracking.py`，按冻结规则（ROI x ∈ [690,780)、y ∈ [380,760)；gray < 120；8 邻域面积 ≥ 150 px；行跨度 ≥ 35 px 且连续 ≥ 3 行；带宽 40–75 px、带高 ≤ 15 px）逐帧处理 frame 76–235，产出 `results/EXP-EXT-LAB67-V1_trajectory.csv`（160 行，表头 `frame,time_s,detected,x_px,y0_px,bbox_w_px,bbox_h_px,area_px`）
- 运行前强制视频 SHA-256 校验（不一致即停止）；运行前后视频哈希一致（原始视频未被修改）；内置 15 项轨迹质量检查（行数 / 帧区间 / 连续性 / 检出率 / 空值 / 各量范围 / 最大单帧 Δy₀ / 突跳次数 / 候选歧义 / 时间轴一致性）

### 变更

- 无

### 修复

- 无

### 说明

- 正式轨迹仅包含 frame 76–235（160 帧，5.3333 s），检出率 **160 / 160**；frame 0–73 仅作诊断参考；**frame 74–75 在冻结规则下保持缺失**，未通过放宽规则强行补值
- 正式位置特征为暗带上缘 y₀（不使用面积、质心或下缘）；本阶段仍未建立该外部视频的 px/mm 标定

## [v0.5.0] - 2026-09-25

### 新增

- **M5 动态位移与运动轨迹实验**：新增 `src/dynamic_displacement.py`，完成 EXP-004-DYNAMIC-001 的完整动态数据链（原始视频 → M3 时序坐标 → 有效门限 → 位移方向投影 → s0 → 位移序列 → 转向点统计）
- 新增 `demo/run_m52_dynamic_displacement.py`（动态位移链路入口）与 `demo/run_m53_plot.py`（位移-时间曲线绘制）
- 产出 `results/EXP-004-DYNAMIC-001_track.csv`、`results/EXP-004-DYNAMIC-001_ds.csv`、`results/EXP-004-DYNAMIC-001_displacement.png` 与叠加视频

### 变更

- 无

### 修复

- 无

### 说明

- 冻结标定：以 P1（10 cm）与 P2（20 cm）两段刻线建立像素尺度 → `PX_PER_MM_004 = 6.086399`、`MM_PER_PX_004 = 0.164301`；s0 参考窗口 2.0 s；转向点最小行程 20 px；处理 1766 帧
- 位移结果同时给出像素与毫米两种表达，并保留原始 x_px 转向点统计

## [v0.4.0] - 2026-09-24

### 新增

- **M4 像素尺度标定与静态位移测量**：新增 `src/calibration.py`（像素尺度标定）、`src/displacement.py`（静态位移与位移-时间数据）、`demo/pick_calibration_points.py`（交互式选取标定点）与 `demo/run_m43_displacement.py`
- 产出 `results/EXP-003-STATIC-001/002/003_track.csv` 与对应的 `_ds.csv`（静态位移序列）

### 变更

- 无

### 修复

- 无

### 说明

- EXP-003 静态实验以 60 mm 已知长度建立像素尺度 → `PX_PER_MM = 5.756961`、`MM_PER_PX = 0.173703`；s0 参考窗口 0.5 s
- 该标定服务于本项目自采静态实验（EXP-003）；**未**用于 M6 的外部公开视频

## [v0.3.0] - 2026-09-23

### 新增

- **M3.3 视频逐帧追踪与时序坐标提取**：新增 `src/video_tracker.py`，完成对真实视频 `data/raw/EXP-002-VIDEO-001.mp4` 的视频逐帧目标追踪，首次实现“视频 → 时序坐标数据”
- 逐帧复用 M2.3 的红色目标检测器（`from src.marker_detector import detect_marker`），检测参数保持单一数据源，未复制或重新定义任何 HSV 区间、形态学核大小与最小面积
- 使用从当前视频文件实际读取的 FPS（60.124797，未硬编码），按 `time_s = frame_index / fps` 生成时间轴
- 正常处理全部 952 帧，每帧写入一行时序数据，生成 `results/EXP-002-VIDEO-001_track.csv`（表头 `frame,time_s,x_px,y_px,area_px,detected`）
- 生成人工核查用叠加视频 `results/EXP-002-VIDEO-001_overlay.mp4`（0.5 倍缩放，绿色外接矩形 + 中心点 + 中心坐标文字）
- 新增 `demo/run_video_tracking.py`：只负责指定输入输出路径、启动追踪流程、打印统计与自检结果
- 叠加视频写入器不可用时自动回退：最多保存 5 张代表性 PNG（起始 / 中间 / 结束 / 最快运动 / 最模糊），CSV 照常完整生成
- 内置 CSV 数据完整性自检：行数与实际解码帧数一致、frame 连续无重复、`detected=True` 行字段齐全、`detected=False` 行字段全为空、坐标不越出画面范围、可被 `np.genfromtxt` 直接读取

### 变更

- `README.md`：当前状态更新为“M3 视频目标逐帧追踪与时序坐标提取完成并通过人工验收”，下一阶段改为“M4 像素尺度标定与实际位移测量”

### 修复

- 无

### 说明

- 实测结果：程序实际处理画面 1080 × 1920（已显式开启 `CAP_PROP_ORIENTATION_AUTO`，未手工旋转、未交换 x/y）；FPS 60.124797；952 / 952 帧处理完成；检出成功率 100.0%；丢失帧 0；最长连续丢失 0 帧；area 最小 / 中位数 / 最大 = 4673.0 / 5265.0 / 6061.5 px²；时间轴 0.000000 s ~ 15.817101 s
- 原始视频运行前后 SHA-256 一致（未被修改）：`E086F1BB250005AF7EDC97A887C390C0BAC82C48F70E6DC36F40B4F0B146C678`
- 人工验收通过：起点、中点、终点、快速运动帧与模糊帧的检测框和中心点均确认落在红色目标上
- 未引入新的第三方依赖（仍只使用 OpenCV、NumPy、Matplotlib），未使用 YOLO / 深度学习 / 卡尔曼滤波 / 光流等复杂跟踪算法
- 本阶段只做测量与记录，未做位移计算、毫米换算、振动分析与 FFT（属于 M4 及后续阶段）

## [v0.2.0] - 2026-09-23

### 新增

- **M2.3 单张图片视觉检测**：新增 `src/marker_detector.py`，完成对真实原始图片 `data/raw/EXP-001-IMAGE-001.jpg` 的红色目标检测
- HSV 红色双区间分割：红色在 HSV 色环上位于两端，用 `[0,100,70]~[10,255,255]` 与 `[170,100,70]~[179,255,255]` 两个区间分别取掩膜再合并
- 3×3 开运算去除零散噪点，保留目标主体形状
- 外部轮廓检测（`RETR_EXTERNAL`）并按面积筛选最大轮廓，面积小于阈值的候选直接忽略
- 用图像矩计算轮廓质心，输出目标中心像素坐标、轮廓面积与外接矩形
- 新增 `demo/run_single_image_detection.py`，生成并保存 `results/EXP-001-IMAGE-001_overlay.png`（标注图）与 `results/EXP-001-IMAGE-001_mask.png`（分割掩膜）

### 变更

- 无

### 修复

- 处理 Windows 中文路径读取问题：`cv2.imread` / `cv2.imwrite` 在含中文的路径下会失败，改用 `np.fromfile` + `cv2.imdecode` 读取、`cv2.imencode` + `tofile` 保存

### 说明

- 用真实拍摄图片完成人工验收：overlay、mask 与中心位置均正确
- 未引入额外第三方依赖，仍只使用 OpenCV、NumPy、Matplotlib
- 本次回归实测结果：中心坐标 (512.7, 964.5) px，面积 1278.5 px，外接矩形 (494, 940, 37, 51)

## 版本记录模板（复制此块用于新版本）

```text
## [v0.x.y] - YYYY-MM-DD

### 新增

- 

### 变更

- 

### 修复

- 

### 说明

- 
```
